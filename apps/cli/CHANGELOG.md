# @launchkey/cli

## 0.2.0

### Minor Changes

- Rename the published package to `@launchkey/cli` and expose the `launchkey` executable.
- Rename the default config path to `~/.launchkey/config.toml`.
- Rename LaunchKey environment variables to `LAUNCHKEY_CONFIG_PATH`, `LAUNCHKEY_TELEMETRY`, and `LAUNCHKEY_OTLP_ENDPOINT`.

### Patch Changes

- Keep the LaunchKey CLI package bundled without runtime npm dependencies.
